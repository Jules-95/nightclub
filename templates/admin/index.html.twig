{% extends 'base.html.twig' %}

{% block title %}Dashboard Admin{% endblock %}

{% block body %}
<div class="max-w-4xl mx-auto mt-10 p-6">
    <h1 class="text-2xl font-bold mb-6">Dashboard Admin</h1>

    <div class="grid grid-cols-3 gap-4">
        <a href="{{ path('app_soiree_index') }}" class="bg-white rounded-xl shadow p-6 hover:bg-gray-50">
            <h2 class="text-lg font-semibold">🎉 Soirées</h2>
            <p class="text-gray-500 text-sm">Gérer les soirées</p>
        </a>
        <a href="{{ path('app_artiste_index') }}" class="bg-white rounded-xl shadow p-6 hover:bg-gray-50">
            <h2 class="text-lg font-semibold">🎵 Artistes</h2>
            <p class="text-gray-500 text-sm">Gérer les artistes</p>
        </a>
        <a href="{{ path('app_materiel_index') }}" class="bg-white rounded-xl shadow p-6 hover:bg-gray-50">
            <h2 class="text-lg font-semibold">🔧 Matériel</h2>
            <p class="text-gray-500 text-sm">Gérer le matériel</p>
        </a>
    </div>
</div>
{% endblock %}
