{% extends 'base.html.twig' %}

{% block title %}Nouvel artiste{% endblock %}

{% block body %}
<div class="max-w-lg mx-auto mt-10 p-6 bg-white rounded-xl shadow">

    <div class="flex justify-between items-center mb-6">
        <h1 class="text-2xl font-bold">Ajouter un artiste</h1>
        {# Lien retour vers la liste #}
        <a href="{{ path('app_artiste_index') }}" class="text-gray-500 hover:text-gray-700 text-sm">
            ← Retour à la liste
        </a>
    </div>

    {# Formulaire de création #}
    {{ include('artiste/_form.html.twig') }}

</div>
{% endblock %}
